import 'package:flutter/material.dart';

class GridTileDemo extends StatelessWidget {
  const GridTileDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('SnippetGridTile');
  }
}
