import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ExpansionPanelListProps {
}

export const ExpansionPanelList = (props: ExpansionPanelListProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
