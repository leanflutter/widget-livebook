import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface PopupMenuButtonProps {
}

export const PopupMenuButton = (props: PopupMenuButtonProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
