import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ClipRectProps {
}

export const ClipRect = (props: ClipRectProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
