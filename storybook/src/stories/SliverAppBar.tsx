import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface SliverAppBarProps {
}

export const SliverAppBar = (props: SliverAppBarProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
