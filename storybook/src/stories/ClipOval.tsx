import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ClipOvalProps {
}

export const ClipOval = (props: ClipOvalProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
