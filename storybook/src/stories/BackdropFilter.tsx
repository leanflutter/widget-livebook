import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface BackdropFilterProps {
}

export const BackdropFilter = (props: BackdropFilterProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
