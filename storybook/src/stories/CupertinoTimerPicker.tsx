import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface CupertinoTimerPickerProps {
}

export const CupertinoTimerPicker = (props: CupertinoTimerPickerProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
