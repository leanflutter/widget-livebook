import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface GridTileBarProps {
}

export const GridTileBar = (props: GridTileBarProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
