import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface InkWellProps {
}

export const InkWell = (props: InkWellProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
