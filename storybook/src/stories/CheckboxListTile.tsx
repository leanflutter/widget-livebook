import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface CheckboxListTileProps {
}

export const CheckboxListTile = (props: CheckboxListTileProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
