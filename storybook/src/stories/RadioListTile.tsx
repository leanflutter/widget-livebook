import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface RadioListTileProps {
}

export const RadioListTile = (props: RadioListTileProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
