import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface AnimatedPositionedProps {
}

export const AnimatedPositioned = (props: AnimatedPositionedProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
