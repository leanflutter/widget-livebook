import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface RefreshIndicatorProps {
}

export const RefreshIndicator = (props: RefreshIndicatorProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
