import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface PositionedTransitionProps {
}

export const PositionedTransition = (props: PositionedTransitionProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
