import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface PositionedProps {
}

export const Positioned = (props: PositionedProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
