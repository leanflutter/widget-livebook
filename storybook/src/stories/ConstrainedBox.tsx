import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ConstrainedBoxProps {
}

export const ConstrainedBox = (props: ConstrainedBoxProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
