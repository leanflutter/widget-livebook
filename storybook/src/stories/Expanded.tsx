import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ExpandedProps {
}

export const Expanded = (props: ExpandedProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
