import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface AnimatedAlignProps {
}

export const AnimatedAlign = (props: AnimatedAlignProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
