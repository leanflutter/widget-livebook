import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface CupertinoPickerProps {
}

export const CupertinoPicker = (props: CupertinoPickerProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
