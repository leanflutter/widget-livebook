import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface DefaultTextStyleProps {
}

export const DefaultTextStyle = (props: DefaultTextStyleProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
