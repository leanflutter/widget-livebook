import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface CupertinoSliderProps {
}

export const CupertinoSlider = (props: CupertinoSliderProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
