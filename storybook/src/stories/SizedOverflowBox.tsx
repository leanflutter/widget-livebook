import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface SizedOverflowBoxProps {
}

export const SizedOverflowBox = (props: SizedOverflowBoxProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
