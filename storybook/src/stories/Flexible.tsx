import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface FlexibleProps {
}

export const Flexible = (props: FlexibleProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
