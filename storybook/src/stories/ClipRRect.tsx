import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ClipRRectProps {
}

export const ClipRRect = (props: ClipRRectProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
