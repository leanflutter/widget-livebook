import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ListTileProps {
}

export const ListTile = (props: ListTileProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
