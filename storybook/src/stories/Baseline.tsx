import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface BaselineProps {
}

export const Baseline = (props: BaselineProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
