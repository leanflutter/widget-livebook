import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface RotationTransitionProps {
}

export const RotationTransition = (props: RotationTransitionProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
