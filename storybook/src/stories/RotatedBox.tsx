import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface RotatedBoxProps {
}

export const RotatedBox = (props: RotatedBoxProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
