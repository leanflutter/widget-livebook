import '../../includes.dart';

class SliceDataTable extends StatelessWidget implements SliceExample {
  @override
  String get name => 'SliceDataTable';

  @override
  Widget build(BuildContext context) {
    return Text(
      'SliceDataTable'
    );
  }
}
