import '../../includes.dart';

class SliceTextSelection extends StatelessWidget implements SliceExample {
  @override
  String get name => 'SliceTextSelection';

  @override
  Widget build(BuildContext context) {
    return Text(
      'SliceTextSelection'
    );
  }
}
