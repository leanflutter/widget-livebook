import '../../includes.dart';

class SliceGridTile extends StatelessWidget implements SliceExample {
  @override
  String get name => "SliceGridTile";

  @override
  Widget build(BuildContext context) {
    return Text(
      'SliceGridTile'
    );
  }
}
