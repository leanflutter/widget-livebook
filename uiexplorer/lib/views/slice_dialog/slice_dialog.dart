import '../../includes.dart';

class SliceDialog extends StatelessWidget implements SliceExample {
  @override
  String get name => 'SliceDialog';

  @override
  Widget build(BuildContext context) {
    return Text(
      'SliceDialog'
    );
  }
}
