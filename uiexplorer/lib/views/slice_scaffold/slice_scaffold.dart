import '../../includes.dart';

class SliceScaffold extends StatelessWidget implements SliceExample {
  @override
  String get name => 'SliceScaffold';

  @override
  Widget build(BuildContext context) {
    return Text(
      'SliceScaffold'
    );
  }
}
